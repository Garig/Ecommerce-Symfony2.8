{% extends '::layout/layoutAdmin.html.twig' %}

{% block body -%}
    <h1>Corbeille</h1>

    <table class="table table-striped table-hover">
        <thead>
            <tr>
                <th>Titre</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td>{{ entity.titre }}</td>
                <td>
                    <a href="{{ path('adminPages_restore', { 'id': entity.id }) }}"><i class="fa fa-pencil"></i>&nbsp;restaurer</a>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('adminPages_index') }}">
                retour
            </a>
        </li>
    </ul>
{% endblock %}
